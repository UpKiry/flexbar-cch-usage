# Flexbar CC Hub 用量插件计划

## 目标

在 Flexbar 上稳定显示 CC Hub 配额和用量，配置一次即可长期使用。

## 当前版本：1.3.1

### 功能范围

- 两个按键：配额、用量。
- 配额固定显示 5 小时的当前使用金额 / 上限。
- 用量支持 `5h`、`1d`、`7d`、`1m`，默认 `1d`，按键独立保存。
- 配置页支持 URL、API Key、刷新间隔和连接测试。
- 测试连接不保存；保存后重启插件并立即刷新。
- 配置保留恢复副本，重新安装后尝试恢复。
- 使用 PNG 自绘和内嵌 CJK 字体，避免中文乱码。
- 启动和重连时先取数再绘制，避免显示空值；丢弃旧设备的绘制任务。

### 交付状态

- [x] 配置、双按键和四种时间范围。
- [x] CC Hub 登录、用量请求、重试、错误脱敏和旧数据保留。
- [x] 多设备隔离、刷新并发控制和绘制去重。
- [x] 配置持久化、测试连接和保存后刷新。
- [x] 启动顺序、重连绘制和中文 PNG 修复。
- [x] 自动化测试、构建、插件校验和打包。
- [x] 已安装到本机 FlexDesigner。
- [ ] 完成真实 Flexbar 冷启动、拔插和长时间运行验证。

### 验收标准

- 冷启动后配额和用量按键能显示实际数值。
- 切换用量范围后，配置可持久化且显示对应数据。
- API 失败时保留旧数据并标记过期，不泄露密钥或 Cookie。
- 保存配置后插件重启，按键在下一次刷新中显示新数据。
- 设备重连和多设备同时使用时不崩溃、不串数据。

### 自动化验证

```bash
npm test
npm run build
npm run plugin:validate
npm run plugin:pack
```

开发分支为 `feature/two-key-usage-range`，审查通过后再合并主线和发布版本。

## 后续版本

### V2

- 最近调用记录。
- 模型和成本明细。
- 按键点击查看详情。

### V3

- 配额进度和阈值提示。
- 刷新倒计时与连接状态。
- 多账户或多 CC Hub 配置。
