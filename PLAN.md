# Flexbar CC Hub 用量插件计划

## 产品目标

在 Flexbar 上提供一个稳定、低频刷新的 CC Hub 用量仪表盘。用户完成一次配置后，可以在设备上快速查看配额、今日用量和近期汇总，不需要打开网页或命令行。

## V1：可靠仪表盘

### 功能范围

- 插件配置页：CC Hub URL、API Key、刷新间隔、汇总天数；
- 四个固定按键：总览、5 小时配额、今日用量、近期用量；
- 启动、设备连接、按键点击和定时自动刷新；
- 保留上一次成功数据，并在数据过期时给出状态提示；
- 使用 CC Hub 登录会话获取用量数据；
- 配置和 API Key 由 FlexDesigner 管理，不写入按键内容或日志。

### V1 交付任务

- [x] 完成配置页和配置持久化；
- [x] 完成四个固定按键的基础显示；
- [x] 接入配额、今日用量和日期汇总接口；
- [x] 完成启动、点击、设备连接和定时刷新；
- [x] 完成 Cookie 复用、重新登录和有限重试；
- [x] 完成错误状态和旧数据保留；
- [x] 使用内嵌 CJK 字体和按键级 PNG 自绘，固定布局为 240×60；
- [x] 按设备和按键视觉指纹去重，数据不变时跳过图片发送；
- [x] 覆盖 180px 紧凑布局、极端数据和字体字符集测试；
- [ ] 补充 CC Hub 客户端 mock 测试；
- [ ] 补充刷新并发、缓存和配置更新测试；
- [ ] 补充 FlexDesigner 事件和 `plugin.draw()` mock 测试；
- [ ] 完成设备拔插、多设备和长时间运行验收。
- [ ] 在 macOS 实际 Flexbar 上确认自绘 PNG 的安装、刷新和最终字形效果。

## V1 数据接口

插件使用以下 CC Hub 接口：

- `POST /api/auth/login`
- `GET /api/v1/me/quota`
- `GET /api/v1/me/today`
- `GET /api/v1/me/usage-logs/stats-summary`

第一版不在常驻按键中展示完整调用日志。

## V1 验收标准

- 配置页可以保存并重新打开后保持原值；
- 正确的 CC Hub URL 和 API Key 可以显示实际用量；
- 四个按键标题在真实 Flexbar 上保持可读；
- API 失败时不清空旧数据，也不泄露 API Key 或 Cookie；
- 插件重启、设备重连和配置更新后能够恢复刷新；
- 同时连接多个设备时，各设备按键状态互不覆盖；
- 插件可在 macOS 和 Windows FlexDesigner 上安装运行。

## V2：详情信息

在 V1 稳定后增加：

- 最近调用记录；
- 模型分布；
- 点击按键查看详细信息；
- 可配置日期范围和刷新间隔。

## V3：视觉与账户扩展

在 V2 稳定后评估：

- 动态按键；
- 配额进度和阈值视觉提示；
- 刷新倒计时和连接状态；
- 多账户或多 CC Hub 配置。

## 完成定义

V1 的实现、自动化测试和真实设备验收全部通过后，才标记为完成并进入 V2。版本号只在可安装包完成验证后推进。
